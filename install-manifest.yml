# Install manifest: which skills to install
# Run: ./install.sh to install all skills below
# Or: ./install.sh --skill=<name> to install specific skill

# Where to symlink skills (default: ~/.agents/skills)
# Override with FLATAGENTS_SKILLS_DIR env var
#skills_dir: ~/.agents/skills

skills:
  - codebase-ripper
  - quick-report
  - shell-analyzer
  - website-scraper
  - flatmachine-manager
  - openai-deep-research-citation-fixer
  - coding-tools
# prototype
#  - repo-map
#  - search-refiner
# not typically used as skills, special use only
#  - coding-agent
#  - dynamic-agent
#  - codebase-explorer
#  - socratic-teacher
#  - test-writer
