# Install manifest: which skills to install
# Run: ./install.sh to install all skills below
# Or: ./install.sh --skill=<name> to install specific skill

skills:
  - codebase-ripper
#  - repo-map
#  - search-refiner
  - shell-analyzer
  - website-scraper
  - flatmachine-manager
# not typically used as skills, special use only
#  - coding-agent
#  - dynamic-agent
#  - codebase-explorer
#  - socratic-teacher
#  - test-writer
