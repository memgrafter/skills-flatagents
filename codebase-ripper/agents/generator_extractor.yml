# DEPRECATED — This agent has been removed.
#
# Command parsing is now done in hooks code (CodebaseRipperHooks._parse_commands_from_text),
# not by an LLM call. This eliminates a fragile JSON transform layer between model stages
# and makes the ripper work with any agent adapter (claude-code, codex-cli, pi-agent, etc.)
#
# See: docs/BEST_PRACTICES.md §2 "LLM Handoff Simplicity"
#   "Pass plain text/markdown payloads between LLM stages."
#   "More transformation layers = more failure points."
