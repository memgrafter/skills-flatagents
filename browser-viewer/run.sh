# playwriter MCP

https://github.com/remorses/playwriter

```
{
  "mcpServers": {
    "playwriter": {
      "command": "npx",
      "args": [
        "-y",
        "playwriter@latest"
      ]
    }
  }
}
```

npm i -g playwriter
playwriter -s 1 -e "await page.goto('https://example.com')"
